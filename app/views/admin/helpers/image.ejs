<% 
    imageHelper = (folder, imageName, alt = "Image") => {
        let xhtml = '';

        if(imageName == null) {
            imageName = 'no-avatar.png';
        }
        
        return `<img class="img-responsive img-avatar" src="${folder + imageName}" alt="${alt}">`;
    }
%>